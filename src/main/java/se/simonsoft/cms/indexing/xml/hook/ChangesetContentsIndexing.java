package se.simonsoft.cms.indexing.xml.hook;

/**
 * Probably quite generic logic to identify contents from a changeset and pass it on to indexer.
 * 
 * This would be a good place to decide if previous version should
 * be re-indexed with a head=false field.
 */
public interface ChangesetContentsIndexing {

}
